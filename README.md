# 간다GO — 송파 출장마사지·홈타이 안내 사이트

송파구 전지역 방문 관리(출장마사지·홈타이) 안내용 정적 사이트입니다.
예약전화: **0508-202-4719**

## 구조

- 정적 HTML 사이트 — 어느 호스팅(GitHub Pages, Netlify, 일반 웹서버)에서든 그대로 서빙 가능
- `build.py` + `content/` 패키지에서 페이지를 생성하는 빌드 방식
- 생성물(각 디렉터리의 `index.html`, `sitemap.xml`, `robots.txt`)도 저장소에 포함

```
build.py            # 빌드 스크립트 (레이아웃·글자수 검사·sitemap 생성)
audit.py            # 배포 전 감사 (중복 타이틀·유사도·도어웨이 URL 검사)
content/
  site.py           # 상호·전화·BASE_URL·메뉴 구조
  main.py           # 메인 페이지 (+ LocalBusiness/FAQPage JSON-LD)
  areas_a.py        # 지역별: 송파구 허브 + 대표 동 7 (풍납~석촌)
  areas_b.py        # 지역별: 대표 동 7 (삼전~신천)
  stations_a.py     # 지하철역별: 허브 + 2·3호선 + 5호선 일부 (11개 역)
  stations_b.py     # 지하철역별: 5호선 올림픽공원 + 8·9호선 (11개 역)
  themes.py         # 테마별: 허브 + 14개 테마
  info.py           # 출장마사지 안내·코스·예약·가이드·후기·고객센터·약관
  magazine.py       # 매거진 허브 + 아티클 6편
  about.py          # 운영자 소개 (E-E-A-T)
  pricing.py        # 공용 요금 블록
assets/             # CSS, 모바일 내비 JS, 파비콘·OG 이미지
```

## 빌드

```bash
python3 build.py    # 페이지 생성 + 글자수 리포트
python3 audit.py    # 배포 전 감사
```

## SEO 운영 원칙 (빌드에 강제됨)

- 본문 **2,000자 미만 페이지는 자동 `noindex`** 처리되고 sitemap에서 제외
- 지역은 대표 동 14개만 — 숫자 행정동(풍납1동, 잠실3동 등) 페이지 없음
  - 풍납1·2동→풍납동 / 거여1·2동→거여동 / 마천1·2동→마천동 / 방이1·2동·오륜동→방이동
  - 송파1·2동→송파동 / 가락본동·가락1·2동→가락동 / 문정1·2동→문정동
  - 잠실본동·잠실2·3·7동→잠실동 / 잠실4·6동→신천동 / 장지동·위례동은 각각 독립
- 역은 역 1개당 페이지 1개 (22개 역) — 환승역(잠실·종합운동장·가락시장·오금·석촌·올림픽공원)도 URL 하나, 출구별 페이지 없음
- **지역+역+테마 조합 페이지 없음** (도어웨이 방지) — 테마는 독립 페이지로만 운영
- 상단/하위 메뉴와 푸터에 키워드·지역명·역명 대량 나열 없음
- 모든 페이지 본문은 페이지별 고유 작성 (지역명만 바꾼 복붙 없음)

## 배포 전 해야 할 일

1. `content/site.py`의 `BASE_URL`을 실제 도메인으로 변경
2. `python3 build.py` 재실행 (canonical·sitemap·robots.txt에 반영됨)
3. `python3 audit.py` 통과 확인
4. Google Search Console에 `sitemap.xml` 제출 (네이버 서치어드바이저 동일)
